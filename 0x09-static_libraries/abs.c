#include <stdio.h>
#include "main.h"

/**
 * _abs - things happen wow
 *
 * @n: number (is supposedly not supposed to have a var??)
 *
 * Return: stuff
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}

	return (0);
}
